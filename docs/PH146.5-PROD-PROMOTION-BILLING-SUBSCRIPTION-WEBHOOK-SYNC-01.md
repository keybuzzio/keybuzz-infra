# PH146.5 — PROD Promotion — Billing Subscription Webhook Sync

> Date : 2026-04-13
> Type : promotion controlee DEV -> PROD
> Verdict : **SUBSCRIPTION SYNC PROMOTED TO PROD**

---

## Preflight

| Check | Valeur |
|-------|--------|
| API DEV validee | `v3.5.49-ph146.5-sub-sync-dev` |
| API PROD avant | `v3.5.48-ph145.8-resolve-prod` |
| Fichier modifie | `src/modules/billing/routes.ts` uniquement |
| Changement DB | `ALTER TABLE billing_subscriptions ADD COLUMN current_period_start TIMESTAMPTZ` |
| 0 fichier Studio | Confirme |
| 0 changement hors scope | Confirme |

---

## Action Appliquee

### Image API PROD

| | Valeur |
|--|--------|
| **Avant** | `ghcr.io/keybuzzio/keybuzz-api:v3.5.48-ph145.8-resolve-prod` |
| **Apres** | `ghcr.io/keybuzzio/keybuzz-api:v3.5.49-ph146.5-sub-sync-prod` |

### Migration DB PROD

```sql
ALTER TABLE billing_subscriptions ADD COLUMN IF NOT EXISTS current_period_start TIMESTAMPTZ;
-- Result: ALTER TABLE (success)
-- Non-destructif: ADD COLUMN IF NOT EXISTS
```

Schema final :

```
tenant_id, stripe_subscription_id, plan, billing_cycle,
channels_included, channels_addon_qty, status,
current_period_end, updated_at, created_at,
has_agent_keybuzz_addon, current_period_start
```

### Manifest modifie

`k8s/keybuzz-api-prod/deployment.yaml`

---

## Diff Minimal (routes.ts)

```diff
# BillingData interface
+  currentPeriodStart?: string | null;

# SELECT queries
-  s.status, s.current_period_end
+  s.status, s.current_period_end, s.current_period_start
+  currentPeriodStart: row.current_period_start?.toISOString() || null,

# handleSubscriptionChange — period extraction
+  const firstItem = subscription.items?.data?.[0];
+  const periodEnd = (subscription as any).current_period_end
+    || (firstItem as any)?.current_period_end
+    || subscription.trial_end || null;
+  const periodStart = (subscription as any).current_period_start
+    || (firstItem as any)?.current_period_start
+    || subscription.trial_start || null;

# INSERT/UPDATE query
-  current_period_end)
+  current_period_end, current_period_start)
+  current_period_start = to_timestamp($10),
-  subscription.current_period_end,
+  periodEnd, periodStart,
```

---

## Backfill PROD

```
=== PH146.5 BACKFILL PROD COMPLETE ===
Updated: 5
Already OK: 0
Skipped: 0
Errors: 0
Total: 5
```

| Tenant | Status | period_end | period_start |
|--------|--------|------------|--------------|
| switaa-sasu-mnc1ouqu | past_due | 2026-05-12 | 2026-04-12 |
| compta-ecomlg-gmail--mnvu4649 | trialing | 2026-04-26 | 2026-04-12 |
| ecomlg-mn3rdmf6 | trialing | 2026-04-26 | 2026-04-12 |
| switaa-sasu-mn9c3eza | active | 2026-05-10 | 2026-04-10 |
| romruais-gmail-com-mn7mc6xl | canceled | 2026-04-09 | 2026-03-26 |

NULL period_end : 0/5
NULL period_start : 0/5

---

## Validation Technique PROD

| Test | Resultat |
|------|----------|
| Pod Running | 1/1 Ready |
| Image confirmee | `v3.5.49-ph146.5-sub-sync-prod` |
| `/health` | `{"status":"ok"}` |
| `/billing/current` compta-ecomlg | `currentPeriodEnd: "2026-04-26T14:10:22.000Z"` |
| `/billing/current` switaa active | `currentPeriodEnd: "2026-05-10T20:11:49.000Z"` |

---

## Validation Produit Reelle

Compte : `compta.ecomlg@gmail.com` sur `https://client.keybuzz.io`

| Page | Resultat |
|------|----------|
| `/billing/history` | **"Prochaine echeance le 26 avril 2026"** — date affichee correctement |
| Montant | 497,00 EUR HT/mois — correct |
| Resume | Autopilot / 1/5 Canaux / Mensuel / Essai |
| Portail Stripe | bouton present et fonctionnel |

---

## Non-Regression PROD

| Endpoint | Resultat |
|----------|----------|
| Health | PASS |
| Billing current (trialing) | PASS — dates presentes |
| Billing current (active) | PASS — dates presentes |
| Conversations API | HTTP 200 PASS |
| Dashboard API | HTTP 200 PASS |
| KBActions wallet | HTTP 200 PASS |
| Orders API | HTTP 200 PASS |
| Dashboard UI | OK |
| Channels UI | OK |
| Billing Hub UI | OK |

**10/10 PASS**

---

## Anti-Contamination

- 0 fichier Studio
- 0 changement hors scope
- 0 regression detectee
- DEV et PROD alignes sur v3.5.49

---

## Rollback

```bash
kubectl set image deploy/keybuzz-api keybuzz-api=ghcr.io/keybuzzio/keybuzz-api:v3.5.48-ph145.8-resolve-prod -n keybuzz-api-prod
kubectl rollout status deployment/keybuzz-api -n keybuzz-api-prod --timeout=120s
```

La colonne `current_period_start` peut rester (ignoree par l'ancien code).

---

## Verdict

### SUBSCRIPTION SYNC PROMOTED TO PROD

PROD promue avec succes. Aucune autre correction effectuee.
