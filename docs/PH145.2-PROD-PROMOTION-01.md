# PH145.2-PROD-PROMOTION-01 — Rapport de Promotion PROD

> Date : 11 avril 2026
> Phase : PH145.2-PROD-PROMOTION-01
> Type : Promotion contrôlée DEV → PROD
> Verdict : **PH145.2 PROMOTED TO PROD**

---

## 1. Contexte

Le rapport PH145.2-AUTOPILOT-UI-PIPELINE-RECOVERY-01 a identifié une cause racine unique :
`evaluateAndExecute()` n'était jamais appelé depuis le pipeline inbound.

Le fix minimal a été validé en DEV : 1 import + 2 appels fire-and-forget dans
`src/modules/inbound/routes.ts`.

Cette phase promeut ce fix identique en PROD.

---

## 2. Préflight

| Check | Résultat |
|---|---|
| API DEV actuelle | `v3.5.241-ph145.2-inbound-autopilot-dev` |
| API PROD avant | `v3.5.240-ph145-guardrails-prod` |
| Source du fix | `src/modules/inbound/routes.ts` sur bastion |
| Diff PH145.2 vs pré-patch | 1 import + 2 blocs fire-and-forget (13 lignes) |
| Fichiers hors scope | 0 |
| Contamination Studio | 0 |

### Diff exact (v3.5.240 → v3.5.241)

```diff
+import { evaluateAndExecute } from '../autopilot/engine';

 // dans /inbound/email (après playbook evaluation)
+        if (body.tenantId && conversationId) {
+          evaluateAndExecute(conversationId, body.tenantId, 'inbound')
+            .catch(err => console.error('[Autopilot] Inbound auto-evaluate error:', err.message));
+        }

 // dans Amazon forward (après playbook evaluation)
+        if (inboundPayload.tenantId && conversationId) {
+          evaluateAndExecute(conversationId, inboundPayload.tenantId, 'inbound')
+            .catch(err => console.error('[Autopilot] Amazon inbound auto-evaluate error:', err.message));
+        }
```

---

## 3. Rollback

| Élément | Valeur |
|---|---|
| Image rollback | `ghcr.io/keybuzzio/keybuzz-api:v3.5.240-ph145-guardrails-prod` |
| Procédure | `kubectl set image deployment/keybuzz-api keybuzz-api=ghcr.io/keybuzzio/keybuzz-api:v3.5.240-ph145-guardrails-prod -n keybuzz-api-prod` |

---

## 4. Build PROD

| Élément | Valeur |
|---|---|
| Tag | `ghcr.io/keybuzzio/keybuzz-api:v3.5.241-ph145.2-inbound-autopilot-prod` |
| Digest | `sha256:80e8224bd8f715eb419a83d5c05a0cc7090902120bdc2593dd936766d978036d` |
| Build | `docker build --no-cache` sur bastion |
| Push GHCR | OK |

---

## 5. Déploiement PROD

| Étape | Résultat |
|---|---|
| `kubectl set image` | OK |
| Rollout status | `successfully rolled out` |
| Pod | `keybuzz-api-bdfb8479d-5m6l4` — 1/1 Running, 0 restarts |
| Ancien pod | Terminé proprement |

---

## 6. Validation Technique

| Check | Résultat |
|---|---|
| Pod Running | 1/1 |
| Image confirmée | `v3.5.241-ph145.2-inbound-autopilot-prod` |
| `/health` | `{"status":"ok"}` — HTTP 200 |
| `GET /autopilot/draft` | HTTP 200 |
| `POST /autopilot/draft/consume` | HTTP 400 (paramètres requis — attendu) |
| `GET /autopilot/settings` | HTTP 200 — retourne config |
| `GET /autopilot/history` | HTTP 200 — retourne historique |
| `evaluateAndExecute` dans compiled routes | 2 occurrences confirmées |

---

## 7. Smoke Tests Métier

### Tenant AUTOPILOT (`switaa-sasu-mnc1ouqu`)

| Test | Résultat |
|---|---|
| Draft existant | `hasDraft: true` |
| Confidence | 0.75 |
| Longueur draft | 809 caractères |
| Log ID | `alog-1775894578241-vql01ah37` |
| `POST /autopilot/evaluate` (conversation sans inbound récent) | `executed: false, reason: LAST_MESSAGE_NOT_INBOUND` (attendu) |

### Non-régression (`ecomlg-001`)

| Test | Résultat |
|---|---|
| Conversations | 50 retournées — OK |
| AI Wallet | OK |
| Orders | OK |
| API logs | Requêtes complétées normalement, 0 erreur |

---

## 8. Anti-contamination

| Check | Résultat |
|---|---|
| Fichiers Studio | 0 |
| Fichiers Client | 0 |
| Fichiers Amazon/connecteurs | 0 |
| Changements hors scope | 0 |

---

## 9. GitOps

| Élément | Valeur |
|---|---|
| Commit | `ed237d6` |
| Fichiers | `k8s/keybuzz-api-dev/deployment.yaml`, `k8s/keybuzz-api-prod/deployment.yaml`, `docs/PH145.2-AUTOPILOT-UI-PIPELINE-RECOVERY-01.md` |
| Push | `14b28d0..ed237d6 main -> main` |
| Remote | `github.com/keybuzzio/keybuzz-infra.git` |

---

## 10. Versions Finales

| Service | DEV | PROD |
|---|---|---|
| API | `v3.5.241-ph145.2-inbound-autopilot-dev` | `v3.5.241-ph145.2-inbound-autopilot-prod` |
| Client | `v3.5.48-white-bg-dev` | `v3.5.48-white-bg-prod` |
| Backend | `v1.0.38-vault-tls-dev` | `v1.0.38-vault-tls-prod` |

DEV et PROD sont alignés.

---

## 11. Verdict

**PH145.2 PROMOTED TO PROD**

Le pipeline complet est désormais actif en PROD :

```
message entrant → inbound/routes.ts → evaluateAndExecute() → draft autopilot → drawer UI
```

Aucune autre correction effectuée dans cette phase.
