# PH36.4 — Octopia New Messages Not Imported (DEV ONLY)

**Date**: 2026-02-22
**Environnement**: DEV uniquement
**Statut**: DIAGNOSTIC COMPLET — STOP POINT AVANT PATCH

---

## 1. Symptôme

Les nouvelles discussions Octopia n'apparaissent plus dans KeyBuzz DEV depuis le 14 février 2026.
- Dernière conversation créée en DB: **2026-02-14T11:53:14Z**
- Dernière activité message (outbound reply): **2026-02-18T23:00 UTC**
- Côté Octopia API: **50+ discussions mises à jour depuis le 14 février**, dont une création du **20 février**

## 2. Vérification côté Octopia (API directe)

Test effectué depuis le pod API DEV (`keybuzz-api-5597d5c569-qpkww`):

| Test | Résultat |
|------|----------|
| Auth OAuth2 (`client_credentials`) | 200 OK — Token Bearer obtenu (TTL: 7200s) |
| `GET /discussions?pageSize=5` | 200 OK — 5 items retournés |
| `GET /discussions?updatedSince=2026-02-14` | 200 OK — **50 items** retournés |

**Discussion la plus récente côté Octopia**:
- `discussionId`: `69980136c20eef4b27720b5b`
- `subject`: "Je souhaite me rétracter"
- `orderReference`: `2601210737K2UJF`
- `createdAt`: 2026-02-20T06:36:28Z
- `updatedAt`: 2026-02-22T03:48:05Z
- `isOpen`: true
- `customerId`: `0000001C6I52`

**Cette discussion N'EXISTE PAS dans KeyBuzz.**

## 3. Vérification côté KeyBuzz DEV (DB)

```
Conversations octopia en DB: 232
Messages octopia en DB: 690 (668 inbound SYSTEM + 22 outbound HUMAN)
MAX(created_at): 2026-02-14T11:53:14Z
MAX(updated_at): 2026-02-19T11:41:02Z (mise à jour statut uniquement)
MAX(last_inbound_at): 2026-02-18T14:24:14Z
```

**Sources des messages**:
| message_source | direction | count |
|----------------|-----------|-------|
| SYSTEM | inbound | 668 |
| HUMAN | outbound | 22 |

Les 668 messages inbound ont `metadata.source = "octopia_import"`, confirmant qu'ils ont été importés via le service `octopiaImport.service.ts` et NON via le pipeline email.

## 4. Pipeline Octopia — Architecture attendue (PH35.x)

Le code source contient un pipeline complet d'import de discussions Octopia:

### Fichiers source existants

| Fichier | Rôle |
|---------|------|
| `keybuzz-api/src/workers/octopiaSyncWorker.ts` | Worker périodique (10 min) |
| `keybuzz-api/src/modules/marketplaces/octopia/octopiaImport.service.ts` | Import/sync/backfill |
| `keybuzz-api/src/modules/marketplaces/octopia/octopia.routes.ts` | Routes API réelles |
| `keybuzz-api/src/modules/marketplaces/octopia/octopiaClient.ts` | Client HTTP Octopia |
| `keybuzz-api/src/modules/marketplaces/octopia/octopiaAuth.service.ts` | OAuth Octopia |
| `keybuzz-api/src/modules/marketplaces/octopia/octopiaOrders.service.ts` | Import commandes |
| `keybuzz-api/src/modules/marketplaces/octopia/octopiaStatus.service.ts` | Gestion connexion |
| `keybuzz-api/src/modules/marketplaces/octopia/types.ts` | Types TypeScript |

### Routes API attendues

| Endpoint | Fonction |
|----------|----------|
| `POST /marketplaces/octopia/import` | Import simple (PH35.1) |
| `POST /marketplaces/octopia/backfill` | Backfill complet paginé (PH35.3) |
| `POST /marketplaces/octopia/sync` | Sync incrémental `updatedSince` (PH35.3) |

### Worker attendu (`octopiaSyncWorker.ts`)

- Intervalle: **10 minutes** (`setInterval`)
- Délai initial: 30 secondes
- Pour chaque tenant actif: appelle `syncOctopiaDiscussions()`
- `syncOctopiaDiscussions()` calcule `updatedSince` = `MAX(updated_at)` depuis la DB
- Pagination par pages de 50, max 200 pages
- Advisory lock PostgreSQL pour éviter les exécutions concurrentes

## 5. Cause racine — CONFIRMÉE

### Problème 1: Import incorrect du module dans `app.ts`

```typescript
// app.ts ligne 40 — ACTUEL (INCORRECT)
import { octopiaRoutes } from './modules/octopia/routes';

// CORRECT (code PH35.x)
import { octopiaRoutes } from './modules/marketplaces/octopia';
```

- `app.ts` importe depuis `./modules/octopia/routes` (chemin obsolète)
- Le répertoire `keybuzz-api/src/modules/octopia/` **N'EXISTE PLUS** dans le code source
- L'image Docker déployée contient un **fichier stub** à `dist/modules/octopia/routes.js`
- Ce stub ne contient que 5 routes basiques (status, test, connect, disconnect, sync/run-demo)
- Les routes réelles (import, sync, backfill) sont dans `modules/marketplaces/octopia/` mais **jamais compilées**

### Problème 2: Worker jamais démarré dans `app.ts`

```typescript
// app.ts ligne 173 — SEUL worker démarré
startSLABatchWorker();

// MANQUANT — jamais importé ni appelé
import { startOctopiaSyncWorker } from './workers/octopiaSyncWorker';
startOctopiaSyncWorker();
```

### Problème 3: Code jamais compilé dans l'image Docker

Image déployée: `ghcr.io/keybuzzio/keybuzz-api:v3.5.24-ph363-dev`

| Chemin dans l'image | État |
|---------------------|------|
| `/app/dist/modules/octopia/routes.js` | STUB (5 routes basiques) |
| `/app/dist/modules/marketplaces/` | **N'EXISTE PAS** |
| `/app/dist/workers/octopiaSyncWorker.js` | **N'EXISTE PAS** |
| `/app/dist/workers/slaBatchWorker.js` | Présent (seul worker) |

### Chronologie de la panne

1. **PH35.x** : Code d'import Octopia développé dans `modules/marketplaces/octopia/`
2. **Import initial fonctionnel** : 232 conversations importées via un déploiement temporaire ou un script one-shot
3. **PH36.3** : Image API reconstruite (`v3.5.24-ph363-dev`) par patching chirurgical d'une image de base
4. L'image de base ne contenait PAS le module `marketplaces/octopia/` compilé
5. Le worker n'a jamais été enregistré dans `app.ts`
6. **Résultat** : sync discussions interrompu depuis le 14 février

### Distinction sync orders vs sync discussions

| Aspect | Sync Orders | Sync Discussions |
|--------|-------------|-----------------|
| Route | `POST /octopia/sync/run` | `POST /marketplaces/octopia/sync` |
| Code déployé | STUB (retourne `ordersCount: 0`) | **NON COMPILÉ** |
| Worker | Aucun | `octopiaSyncWorker.ts` (non démarré) |
| Erreur connue | `response.orders is not iterable` | Aucune erreur (code jamais exécuté) |
| Impact | Commandes non synchronisées | **Discussions non importées** |

## 6. Hypothèses validées

| Hypothèse | Résultat |
|-----------|---------|
| A. Sync worker ne tourne pas | **CONFIRMÉ** — jamais importé dans `app.ts`, jamais compilé |
| B. updatedSince mal calculé | NON — le code source est correct (`MAX(updated_at)`) |
| C. Import uniquement via backfill | NON — le code source a import, backfill ET sync incrémental |
| D. discussionId mismatch | NON — format `octopia:{discussionId}` cohérent |
| E. Filtre limit incorrect | NON — `PAGE_SIZE = 50`, pagination correcte |
| F. response.messages non parsé | NON — le parsing est correct dans le source |
| G. Import écrase updated_at | NON — `importSingleDiscussion` utilise `updatedAt` Octopia |
| H. API Octopia inaccessible | **NON** — auth OK, discussions API retourne 200 avec données |
| I. Code non compilé/déployé | **CONFIRMÉ** — cause racine principale |

## 7. Plan de correction

### Modifications requises dans `app.ts`

1. **Changer l'import octopiaRoutes** :
   - DE: `import { octopiaRoutes } from './modules/octopia/routes';`
   - VERS: `import { octopiaRoutes } from './modules/marketplaces/octopia';`

2. **Ajouter l'import du worker** :
   - `import { startOctopiaSyncWorker } from './workers/octopiaSyncWorker';`

3. **Démarrer le worker** (après `startSLABatchWorker()`) :
   - `startOctopiaSyncWorker();`

### Rebuild et déploiement

1. Compiler le TypeScript avec les chemins corrigés
2. Rebuild l'image Docker : `ghcr.io/keybuzzio/keybuzz-api:v3.5.25-ph364-dev`
3. Déployer sur DEV uniquement
4. Vérifier les logs `[OCTOPIA-SYNC]` dans les 30 premières secondes
5. Vérifier l'import de nouvelles discussions en DB

### Vérification post-déploiement

```sql
-- Avant patch
SELECT COUNT(*) FROM conversations WHERE channel='octopia'; -- 232

-- Après patch (attendre 30s pour le premier run)
SELECT COUNT(*) FROM conversations WHERE channel='octopia'; -- devrait être > 232

-- Vérifier la discussion du 20 février
SELECT id, subject FROM conversations
WHERE channel='octopia' AND thread_key = 'octopia:69980136c20eef4b27720b5b';
```

## 8. Rollback

```bash
# Revenir à l'image précédente
kubectl -n keybuzz-api-dev set image deployment/keybuzz-api \
  keybuzz-api=ghcr.io/keybuzzio/keybuzz-api:v3.5.24-ph363-dev
```

---

## STOP POINT

**Ce rapport est un diagnostic. Aucune modification n'a été appliquée.**
**Validation requise avant de procéder au patch.**

### Résumé exécutif

| Élément | Valeur |
|---------|--------|
| Cause racine | Code PH35.x non compilé/déployé dans l'image Docker |
| Impact | Aucune discussion Octopia importée depuis le 14 février |
| Discussions manquantes | 50+ (depuis Feb 14) dont 1 nouvelle (Feb 20) |
| API Octopia | Fonctionnelle (auth OK, données disponibles) |
| Risque du fix | Faible — le code source est complet et testé |
| Scope | DEV ONLY |
| PROD impacté | NON — pas de modification PROD |
