# PH153.10.1 — ROLLBACK + MISSING FEATURE PROOF

> Date : 1 mars 2026
> Objectif : Rollback DEV + identifier les fichiers manquants pour /start et Inbox Contexte
> Verdict : **ROLLBACK DONE — MISSING FEATURES IDENTIFIED**

---

## 1. ROLLBACK


| Etape            | Resultat                                              |
| ---------------- | ----------------------------------------------------- |
| Image DEV avant  | `v3.5.74-ph153.10-closure-dev`                        |
| Image PROD       | `v3.5.63-ph151.2-case-summary-clean-prod` (INCHANGEE) |
| Image cible      | `v3.5.63-ph151.2-case-summary-clean-dev`              |
| Rollback execute | OUI                                                   |
| Image DEV finale | `v3.5.63-ph151.2-case-summary-clean-dev`              |
| Pod              | Running 1/1                                           |
| PROD             | INCHANGEE                                             |


---

## 2. ANALYSE /start

### Decouverte : AUCUN fichier manquant

Tous les fichiers onboarding sont **IDENTIQUES** entre release et ph152.6 :


| Fichier                                                   | Release (lignes) | Ph152.6 (lignes) | Diff  |
| --------------------------------------------------------- | ---------------- | ---------------- | ----- |
| `app/start/page.tsx`                                      | 6                | 6                | **0** |
| `src/features/onboarding/components/OnboardingHub.tsx`    | 90               | 90               | **0** |
| `src/features/onboarding/components/OnboardingWizard.tsx` | identique        | identique        | **0** |
| `src/features/onboarding/components/OnboardingBanner.tsx` | identique        | identique        | **0** |
| `src/features/onboarding/storage.ts`                      | identique        | identique        | **0** |
| `src/features/onboarding/types.ts`                        | identique        | identique        | **0** |


### Structure reelle

```
app/start/page.tsx → render <OnboardingHub />
OnboardingHub.tsx → "Checklist statique" (commentaire dans le code)
OnboardingWizard.tsx → Stepper multi-etapes (route /onboarding, PAS /start)
```

### Preuve compile v3.5.63

Le chunk compile `1698-010f00acd6c65952.js` contient :

- `OnboardingHub` — present
- `currentStep:1,channelDetails:{amazon:!1,octopia:!1,shopify:!1}` — present (Wizard, pas Hub)
- `"Bienvenue sur KeyBuzz"` — present

### Conclusion /start

**La page `/start` est identique dans release, ph152.6, ET le compile v3.5.63.** Elle affiche une checklist statique (OnboardingHub), pas un stepper. Le stepper horizontal est dans `/onboarding` (OnboardingWizard), qui est une route differente.

**Classification : M3 — aucun fichier a ajouter.**

La difference perçue par l'utilisateur peut provenir de :

1. Confusion entre `/start` (checklist hub) et `/onboarding` (stepper wizard)
2. Etat local (localStorage) affectant le rendu conditionnel du hub
3. Autre composant (non onboarding) affectant le layout

---

## 3. ANALYSE INBOX / CONTEXTE

### 3.1 — InboxTripane.tsx : ETAT HYBRIDE

L'analyse du chunk compile `app/inbox/page-b8f221ac7fe3962d.js` (139 608 octets) revele que la version de InboxTripane dans v3.5.63 n'est **NI la version release NI la version ph152.6**.

#### Preuve par symboles compiles


| Symbole                    | Release     | Ph152.6        | Compile v3.5.63 | Verdict   |
| -------------------------- | ----------- | -------------- | --------------- | --------- |
| `AutopilotDraft`           | OUI (7 ref) | NON (retire)   | **NON** (0)     | ≠ release |
| `forceRefresh`             | OUI         | NON (retire)   | **NON** (0)     | ≠ release |
| `escalationTarget`         | OUI         | NON (retire)   | **OUI** (1)     | = release |
| `pickup` (filtre)          | OUI         | NON (remplace) | **OUI** (1)     | = release |
| `slaState` / `slaDueAt`    | NON         | OUI (ajoute)   | **OUI** (1)     | = ph152.6 |
| `ConversationActionBar`    | NON         | OUI (ajoute)   | **NON** (0)     | ≠ ph152.6 |
| `PlaybookSuggestionBanner` | NON         | OUI (ajoute)   | **NON** (0)     | ≠ ph152.6 |
| RBAC redirect              | NON         | OUI (ajoute)   | **NON** (0)     | ≠ ph152.6 |
| `to_treat` (filtre)        | NON         | OUI (remplace) | **NON** (0)     | ≠ ph152.6 |


#### Conclusion InboxTripane

Le InboxTripane v3.5.63 est une version **intermediaire** : base release avec 3 modifications chirurgicales :

1. **Retrait** de AutopilotDraft (etat + useEffect + import type) — ~40 lignes
2. **Retrait** de forceRefresh (parametre loadConversationDetail + appel post-reply)
3. **Ajout** de slaState/slaDueAt dans mapApiToLocal — ~4 lignes

Cette version intermediaire n'existe dans **aucune branche Git**. Elle a ete creee par modification sur disque avant le build v3.5.63.

**Classification : M2** — la version release est la plus proche (2 patterns exacts sur 4), mais aucune version Git ne correspond exactement. La creation d'un intermediaire necessite une modification de code controlee.

### 3.2 — Sous-composants Inbox

#### TreatmentStatusPanel.tsx : MODIFIE DANS v3.5.63


| Preuve                                | Resultat    |
| ------------------------------------- | ----------- |
| "Client mecontent" dans compile       | **OUI** (1) |
| "Remboursement sensible" dans compile | **OUI** (1) |
| "Cas complexe" dans compile           | **OUI** (1) |
| "Fournisseur requis" dans compile     | **OUI** (1) |
| "Escaladee" dans compile              | **OUI** (1) |


Ces 5 strings REASON_LABELS sont **exclusifs a la version ph152.6** (absents de release). Leur presence dans le compile v3.5.63 **PROUVE** que TreatmentStatusPanel utilise la version ph152.6.

La version ph152.6 ajoute :

- `REASON_LABELS` map (7 raisons d'escalation avec labels FR)
- Props `escalationStatus` et `escalationReason`
- Rendu conditionnel des badges escalation

**Classification : M1 — requis, prouve.**

#### AgentWorkbenchBar.tsx : VERSION RELEASE CONFIRMEE


| Preuve                                   | Resultat    |
| ---------------------------------------- | ----------- |
| `pickup` (filtre release) dans compile   | **OUI** (1) |
| `to_treat` (filtre ph152.6) dans compile | **NON** (0) |
| "reprendre" (label release) dans compile | **OUI** (1) |


**Classification : M3 — version release confirmee dans v3.5.63.**

#### EscalationPanel.tsx : VERSION RELEASE PROBABLE


| Preuve                           | Resultat                         |
| -------------------------------- | -------------------------------- |
| Release : 174 lignes             | Ph152.6 : 121 lignes (simplifie) |
| `escalation_target` dans compile | **OUI** (1)                      |


La simplification ph152.6 est posterieure au build. La version release (plus longue, avec escalation_target) est probablement la correcte.

**Classification : M3 — version release probable.**

#### Autres sous-composants (0 diff)


| Composant                      | Diff | Classification |
| ------------------------------ | ---- | -------------- |
| `OrderSidePanel.tsx`           | 0    | M3             |
| `SupplierPanel.tsx`            | 0    | M3             |
| `AISuggestionsPanel.tsx`       | 0    | M3             |
| `AssignmentPanel.tsx`          | 0    | M3             |
| `PlaybookSuggestionBanner.tsx` | 0    | M3             |
| `PriorityBadge.tsx`            | 0    | M3             |


### 3.3 — Fichiers ph152.6 ABSENTS du compile (confirmes POSTERIEURS)


| Fichier                             | Present dans compile | Verdict    |
| ----------------------------------- | -------------------- | ---------- |
| `AICaseSummary.tsx`                 | NON                  | Posterieur |
| `AutopilotConversationFeedback.tsx` | NON                  | Posterieur |
| `ConversationActionBar.tsx`         | NON                  | Posterieur |
| `ConversationSummaryBar.tsx`        | NON                  | Posterieur |
| `MessageBubble.tsx`                 | NON                  | Posterieur |
| `MessageFilterToggle.tsx`           | NON                  | Posterieur |
| `messageClassifier.ts`              | NON                  | Posterieur |


---

## 4. TABLEAU M1 / M2 / M3

### M1 — Requis prouve


| #    | Fichier                                                  | Zone  | Preuve                               | Source  |
| ---- | -------------------------------------------------------- | ----- | ------------------------------------ | ------- |
| M1-1 | `src/features/inbox/components/TreatmentStatusPanel.tsx` | Inbox | 5 strings REASON_LABELS dans compile | ph152.6 |


### M2 — Probable mais necessite etat intermediaire


| #    | Fichier                      | Zone  | Probleme                                                                                  | Impact                                                                                                                                   |
| ---- | ---------------------------- | ----- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| M2-1 | `app/inbox/InboxTripane.tsx` | Inbox | Hybride : -AutopilotDraft, -forceRefresh, +slaState. Ni release ni ph152.6 ne correspond. | AutopilotDraft dans release = fetch API inutile. ForceRefresh = refresh force apres reply. SlaState absent = badges priorite incomplets. |


### M3 — Non requis


| Fichier                              | Raison                                                |
| ------------------------------------ | ----------------------------------------------------- |
| Tous fichiers `/start` et onboarding | Identiques dans toutes les versions                   |
| `OrderSidePanel.tsx`                 | 0 diff                                                |
| `SupplierPanel.tsx`                  | 0 diff                                                |
| `AISuggestionsPanel.tsx`             | 0 diff                                                |
| `AssignmentPanel.tsx`                | 0 diff                                                |
| `PriorityBadge.tsx`                  | 0 diff                                                |
| `AgentWorkbenchBar.tsx`              | Release confirmee dans compile (pickup=1, to_treat=0) |
| `EscalationPanel.tsx`                | Release probable (escalation_target=1)                |
| 7 fichiers inbox ph152.6             | Confirmes posterieurs a v3.5.63                       |


---

## 5. PROCHAINE CANDIDATE RECOMMANDEE

### Composition


| Groupe            | Nb     | Fichiers                                                                  |
| ----------------- | ------ | ------------------------------------------------------------------------- |
| Base              | -      | `consolidation/client-v3.5.63-closure-candidate` (PH153.10 = 19 fichiers) |
| **M1 ajoute**     | **1**  | `src/features/inbox/components/TreatmentStatusPanel.tsx` (ph152.6)        |
| **M2 a resoudre** | **1**  | `app/inbox/InboxTripane.tsx` (intermediaire a creer)                      |
| **Total**         | **21** | 19 existants + 1 M1 + 1 M2                                                |


### Strategie pour InboxTripane (M2)

Option A — **Release tel quel** (rapide, ecart mineur) :

- Garde AutopilotDraft (fetch API qui retourne vide = pas d'impact visible)
- Garde forceRefresh (rafraichissement supplementaire apres reply = pas d'impact visible)
- Manque slaState (PriorityBadge sans donnees SLA = impact visuel mineur)
- Ecart : fonctionnel mais pas identique

Option B — **Modification controlee** (3 chirurgies, ecart minimal) :

- Partir de la version release
- Retirer l'import `type AutopilotDraft` et le bloc etat/useEffect associe (~40 lignes)
- Retirer le parametre `forceRefresh` et son appel (~10 lignes)
- Ajouter `slaState` + `slaDueAt` dans mapApiToLocal (~4 lignes)
- Necesssite une autorisation explicite de modification de code

Option C — **Ph152.6 tel quel** (IMPOSSIBLE) :

- ConversationActionBar import → echec build garanti

**Recommandation : Option A pour le prochain build.** Les ecarts sont fonctionnellement neutres (AutopilotDraft = API vide, forceRefresh = refresh supplementaire inoffensif). Si validation humaine detecte un ecart visible, passer a l'Option B dans une phase ulterieure avec autorisation de modification controlee.

### Exclusions strictes confirmees

- `InboxTripane.tsx` de ph152.6 → INTERDIT (ConversationActionBar = build failure)
- 7 fichiers inbox posterieurs → INTERDIT
- Fichiers onboarding → INUTILE (identiques)
- AgentWorkbenchBar/EscalationPanel de ph152.6 → INTERDIT (version release confirmee)

### Risques restants

1. InboxTripane release avec AutopilotDraft → fetch API `/api/autopilot/draft` a chaque changement de conversation (inoffensif si endpoint retourne vide/404)
2. Absence de slaState dans mapApiToLocal → PriorityBadge peut afficher sans donnees SLA (mineur)
3. TreatmentStatusPanel.tsx (M1) importe des props `escalationStatus`, `escalationReason` qui doivent etre passes par InboxTripane → verifier que la version release d'InboxTripane passe bien ces props

---

## 6. CONFIRMATION FINALE


| Verification               | Statut                                                |
| -------------------------- | ----------------------------------------------------- |
| Rollback execute           | OUI — `v3.5.63-ph151.2-case-summary-clean-dev`        |
| DEV finale                 | `v3.5.63-ph151.2-case-summary-clean-dev`              |
| PROD                       | `v3.5.63-ph151.2-case-summary-clean-prod` — INCHANGEE |
| NO BUILD                   | CONFIRME                                              |
| NO DEPLOY (apres rollback) | CONFIRME                                              |
| NO COMMIT                  | CONFIRME                                              |
| NO CODE MODIFICATION       | CONFIRME                                              |


---

## 7. VERDICT

**ROLLBACK DONE — MISSING FEATURES IDENTIFIED**

- 1 fichier M1 identifie (TreatmentStatusPanel.tsx — prouve dans compile)
- 1 fichier M2 identifie (InboxTripane.tsx — etat hybride, aucune version Git ne correspond)
- /start : AUCUN fichier manquant (toutes versions identiques)
- AgentWorkbenchBar/EscalationPanel : version release confirmee dans compile
- 7 fichiers inbox ph152.6 confirmes POSTERIEURS (a exclure)

