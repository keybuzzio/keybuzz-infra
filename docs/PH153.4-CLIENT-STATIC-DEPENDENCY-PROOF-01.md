# PH153.4 — CLIENT STATIC DEPENDENCY PROOF

> Date : 2026-03-01
> Type : preuve statique de dependances, lecture seule
> Impact runtime : AUCUN

---

## 1. Rappel des 10 fichiers Cat A (PH153.3)


| #   | Fichier                                                    |
| --- | ---------------------------------------------------------- |
| 1   | `app/api/shopify/status/route.ts`                          |
| 2   | `app/api/shopify/connect/route.ts`                         |
| 3   | `app/api/shopify/disconnect/route.ts`                      |
| 4   | `app/api/space-invites/resolve/route.ts`                   |
| 5   | `public/marketplaces/shopify.svg`                          |
| 6   | `src/features/inbox/components/AICaseSummary.tsx`          |
| 7   | `src/features/inbox/components/ConversationSummaryBar.tsx` |
| 8   | `src/features/inbox/components/MessageBubble.tsx`          |
| 9   | `src/features/inbox/components/MessageFilterToggle.tsx`    |
| 10  | `src/features/inbox/utils/messageClassifier.ts`            |


---

## 2. Methode

- Base analysee : `release/client-v3.5.220` (`2adbd40`)
- Outil : `grep` (pas `rg` — rg a un probleme d'encodage sur ce repo)
- Verification croisee : image compilee v3.5.63 (chunks, manifests, routes)
- InboxTripane.tsx : 95 264 octets, imports valides (confirme par grep)

---

## 3. Tableau de preuve statique


| Fichier                          | Import direct par  | Import indirect | Reference page/route     | Compile v3.5.63 | Verdict |
| -------------------------------- | ------------------ | --------------- | ------------------------ | --------------- | ------- |
| `shopify/status/route.ts`        | aucun (auto-route) | N/A             | channels page (compile)  | OUI             | **A1**  |
| `shopify/connect/route.ts`       | aucun (auto-route) | N/A             | channels page (compile)  | OUI             | **A1**  |
| `shopify/disconnect/route.ts`    | aucun (auto-route) | N/A             | channels page (compile)  | OUI             | **A1**  |
| `space-invites/resolve/route.ts` | aucun (auto-route) | N/A             | login page (compile)     | OUI             | **A1**  |
| `shopify.svg`                    | aucun (auto-asset) | N/A             | channels page (icon map) | OUI             | **A1**  |
| `AICaseSummary.tsx`              | aucun              | aucun           | aucune                   | NON             | **A3**  |
| `ConversationSummaryBar.tsx`     | aucun              | aucun           | aucune                   | NON             | **A3**  |
| `MessageBubble.tsx`              | aucun              | aucun           | aucune                   | NON             | **A3**  |
| `MessageFilterToggle.tsx`        | aucun              | aucun           | aucune                   | NON             | **A3**  |
| `messageClassifier.ts`           | aucun              | aucun           | aucune                   | NON             | **A3**  |


---

## 4. Focus Inbox (detail)

### InboxTripane.tsx — imports dans release/client-v3.5.220

```
import { OrderSidePanel } from "@/src/features/inbox/components/OrderSidePanel"
import { SupplierPanel } from "@/src/features/inbox/components/SupplierPanel"
import { ContactSupplierModal } from "@/src/features/inbox/components/ContactSupplierModal"
import { AssignmentPanel, AssignmentBadge } from "@/src/features/inbox/components/AssignmentPanel"
import { EscalationPanel, EscalationBadge } from "@/src/features/inbox/components/EscalationPanel"
import { PriorityBadge } from "@/src/features/inbox/components/PriorityBadge"
import { AISuggestionsPanel } from "@/src/features/inbox/components/AISuggestionsPanel"
import { AgentWorkbenchBar } from "@/src/features/inbox/components/AgentWorkbenchBar"
import { TreatmentStatusPanel } from "@/src/features/inbox/components/TreatmentStatusPanel"
import { resolveOrderId } from "@/src/features/inbox/utils/resolveOrderId"
import { getConversationPriority, sortByPriority, getPriorityStats } from "@/src/features/inbox/utils/conversationPriority"
```

### Composants importes vs Cat A


| Composant                  | Importe par InboxTripane ? | Dans release base ? |
| -------------------------- | -------------------------- | ------------------- |
| OrderSidePanel             | OUI                        | OUI                 |
| SupplierPanel              | OUI                        | OUI                 |
| ContactSupplierModal       | OUI                        | OUI                 |
| AssignmentPanel            | OUI                        | OUI                 |
| EscalationPanel            | OUI                        | OUI                 |
| PriorityBadge              | OUI                        | OUI                 |
| AISuggestionsPanel         | OUI                        | OUI                 |
| AgentWorkbenchBar          | OUI                        | OUI                 |
| TreatmentStatusPanel       | OUI                        | OUI                 |
| **AICaseSummary**          | **NON**                    | **NON**             |
| **ConversationSummaryBar** | **NON**                    | **NON**             |
| **MessageBubble**          | **NON**                    | **NON**             |
| **MessageFilterToggle**    | **NON**                    | **NON**             |
| **messageClassifier**      | **NON**                    | **NON**             |


### Preuve supplementaire

Recherche `grep -rl` dans tout `app/` et `src/` de la release base :

- AICaseSummary : **NOT FOUND**
- ConversationSummaryBar : **NOT FOUND**
- MessageBubble : **NOT FOUND**
- MessageFilterToggle : **NOT FOUND**
- messageClassifier : **NOT FOUND**

Recherche dans les chunks compiles v3.5.63 :

- AICaseSummary : **NOT IN CHUNKS**
- ConversationSummaryBar : **NOT IN CHUNKS**
- MessageBubble : **NOT IN CHUNKS**
- MessageFilterToggle : **NOT IN CHUNKS**
- messageClassifier : **NOT IN CHUNKS**

### Conclusion Inbox

Les 5 composants inbox Cat A ne sont :

- ni importes par InboxTripane.tsx
- ni importes par aucun autre fichier de la release base
- ni compiles dans les chunks v3.5.63
- ni references dans aucune page, route ou service

Ils etaient presents sur le disque du bastion comme fichiers untracked lors du build, mais n'ont jamais ete compiles dans l'image v3.5.63.

---

## 5. Focus Shopify (detail)

### Preuve dans le code compile v3.5.63

Extraits du chunk compile `app/channels/page-f5872caffdc80a46.js` :

```
fetch("/api/shopify/status?tenantId=".concat(e))
fetch("/api/shopify/connect", { method: "POST", ... })
fetch("/api/shopify/disconnect", { method: "POST", ... })
shopify: "/marketplaces/shopify.svg"
"Connecter Shopify"
"shopify_connected" / "shopify_error" (URL params)
```

### Source Git vs image compilee


| Element                                   | Dans Git (release base)    | Dans image compilee     |
| ----------------------------------------- | -------------------------- | ----------------------- |
| `app/channels/page.tsx` reference shopify | **NON** (grep = 0)         | **OUI** (chunk compile) |
| `app/api/shopify/*/route.ts`              | **NON** (fichiers absents) | **OUI** (auto-compile)  |
| `public/marketplaces/shopify.svg`         | **NON** (fichier absent)   | **OUI** (copie public/) |
| `app/orders/page.tsx` reference shopify   | **OUI**                    | OUI                     |


### Conclusion Shopify

Les 3 routes shopify et le SVG sont **requis** : la channels page compilee les appelle directement. Cependant, la channels page dans le Git release NE CONTIENT PAS ces references — elle a ete modifiee sur le disque du bastion SANS commit Git. Les routes shopify sont auto-compilees par Next.js (file-system routing).

`src/services/shopify.service.ts` n'est PAS reference et n'est PAS compile. Non requis pour v3.5.63.

---

## 6. Focus space-invites/resolve (detail)

### Preuve dans le code compile v3.5.63

Extrait du chunk compile `app/login/page-1c9db12a74c1ae39.js` :

```
fetch("/api/space-invites/resolve?token=".concat(encodeURIComponent(G)))
```

### Source Git vs image compilee


| Element                                  | Dans Git (release base) | Dans image compilee     |
| ---------------------------------------- | ----------------------- | ----------------------- |
| `app/api/space-invites/resolve/route.ts` | **NON**                 | **OUI** (auto-compile)  |
| Login page appelle resolve               | **NON** (grep = 0)      | **OUI** (chunk compile) |


### Conclusion space-invites/resolve

La route est **requise** : la login page compilee l'appelle directement pour valider les tokens d'invitation. Comme pour shopify, la login page dans le Git release a ete modifiee sur le disque du bastion SANS commit.

---

## 7. Decouverte critique : divergence Git vs disque

L'analyse revele que `release/client-v3.5.220` dans Git n'est PAS identique au code sur le disque du bastion au moment du build v3.5.63. Fichiers confirmes divergents :


| Fichier                      | Git (release)      | Disque (build v3.5.63) |
| ---------------------------- | ------------------ | ---------------------- |
| `app/channels/page.tsx`      | PAS de ref shopify | CONTIENT shopify       |
| `app/login/page.tsx`         | PAS de ref resolve | CONTIENT resolve       |
| `app/inbox/InboxTripane.tsx` | 95KB, .bak present | Probablement modifie   |


La branche release/client-v3.5.220 reste la meilleure base disponible, mais elle n'est pas la source exacte de v3.5.63.

---

## 8. Reclassification finale

### A1 — Requis prouves


| #   | Fichier                                  | Preuve                               |
| --- | ---------------------------------------- | ------------------------------------ |
| 1   | `app/api/shopify/status/route.ts`        | Appele par channels page compile     |
| 2   | `app/api/shopify/connect/route.ts`       | Appele par channels page compile     |
| 3   | `app/api/shopify/disconnect/route.ts`    | Appele par channels page compile     |
| 4   | `app/api/space-invites/resolve/route.ts` | Appele par login page compile        |
| 5   | `public/marketplaces/shopify.svg`        | Reference par channels page icon map |


### A2 — Presents mais non prouves

(aucun)

### A3 — Non requis pour v3.5.63


| #   | Fichier                                                    | Raison                        |
| --- | ---------------------------------------------------------- | ----------------------------- |
| 6   | `src/features/inbox/components/AICaseSummary.tsx`          | Zero import, zero compilation |
| 7   | `src/features/inbox/components/ConversationSummaryBar.tsx` | Zero import, zero compilation |
| 8   | `src/features/inbox/components/MessageBubble.tsx`          | Zero import, zero compilation |
| 9   | `src/features/inbox/components/MessageFilterToggle.tsx`    | Zero import, zero compilation |
| 10  | `src/features/inbox/utils/messageClassifier.ts`            | Zero import, zero compilation |


---

## 9. Future liste candidate revisee

### Base

`release/client-v3.5.220` (`2adbd40`) — 407 fichiers

### Fichiers a ajouter (A1 prouves)

```
app/api/shopify/status/route.ts
app/api/shopify/connect/route.ts
app/api/shopify/disconnect/route.ts
app/api/space-invites/resolve/route.ts
public/marketplaces/shopify.svg
```

### Fichiers a garder en archive seulement (A3)

```
src/features/inbox/components/AICaseSummary.tsx
src/features/inbox/components/ConversationSummaryBar.tsx
src/features/inbox/components/MessageBubble.tsx
src/features/inbox/components/MessageFilterToggle.tsx
src/features/inbox/utils/messageClassifier.ts
```

Ces 5 fichiers restent preserves dans :

- `ph153.2-full-source-capture` (branche archive)
- `ph152.6-client-parity` (branche historique)
- `consolidation/client-v3.5.63-source-of-truth` (branche PH153.1)

### Risque residuel

La branche release/client-v3.5.220 contient des versions DIFFERENTES de certaines pages (channels, login, inbox) par rapport au disque du bastion lors du build v3.5.63. Un rebuild depuis cette branche + 5 fichiers A1 produira une image fonctionnelle mais potentiellement pas IDENTIQUE a v3.5.63.

---

## 10. Confirmation


| Check      | Resultat                                              |
| ---------- | ----------------------------------------------------- |
| NO BUILD   | Confirme                                              |
| NO DEPLOY  | Confirme                                              |
| NO COMMIT  | Confirme                                              |
| DEV image  | `v3.5.63-ph151.2-case-summary-clean-dev` (inchangee)  |
| PROD image | `v3.5.63-ph151.2-case-summary-clean-prod` (inchangee) |


---

## 11. Verdict

**STATIC DEPENDENCY PROOF READY**