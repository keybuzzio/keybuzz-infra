# PH33.15-PROD — Déploiement PROD (billing initial grant KBActions)

**Date** : 2026-02-16  
**Statut** : Déployé PROD — API ONLY  
**Client PROD** : Non touché  
**PROD-APPROVED** : Oui

---

## Tags / Images

| Service | Avant (rollback) | Après (déployé) |
|---|---|---|
| **API PROD** | `v3.4.0-ph3311g-api-prod-1` | `v3.4.1-ph3315-kbactions-grant-prod-1` |
| **API PROD digest** | `sha256:896f31c1...` | `sha256:c096d0d4...` |
| **Client PROD** | Non modifié | Non modifié |

---

## Commande rollback

```bash
kubectl set image deployment/keybuzz-api keybuzz-api=ghcr.io/keybuzzio/keybuzz-api:v3.4.0-ph3311g-api-prod-1 -n keybuzz-api-prod
kubectl rollout restart deployment/keybuzz-api -n keybuzz-api-prod

cd /opt/keybuzz/keybuzz-infra
sed -i 's|image: ghcr.io/keybuzzio/keybuzz-api:.*|image: ghcr.io/keybuzzio/keybuzz-api:v3.4.0-ph3311g-api-prod-1|' k8s/keybuzz-api-prod/deployment.yaml
git add k8s/keybuzz-api-prod/deployment.yaml
git commit -m "ROLLBACK PH33.15-PROD: revert to v3.4.0-ph3311g-api-prod-1"
git push origin main
```

---

## Validation technique PROD

### TEST 1 — Fonctions déployées correctement

```
getPlanIncludedKBActions("PRO"):      1000  ✅
getPlanIncludedKBActions("pro"):      1000  ✅
getPlanIncludedKBActions("AUTOPILOT"): 2000  ✅
getPlanIncludedKBActions("autopilot"): 2000  ✅
getPlanIncludedKBActions("STARTER"):     0  ✅
getPlanIncludedKBActions("starter"):     0  ✅

grantInitialKBActions exported: true  ✅
```

### TEST 2 — Grant + Idempotence (tenant de test `test-ph3315-val`)

```
GRANT #1: SUCCESS — 1000 KBActions
Wallet after GRANT #1: {"remaining":"1000.0000","included_monthly":"1000.0000"}

GRANT #2: IDEMPOTENT — already granted, skipping
Wallet after GRANT #2: {"remaining":"1000.0000","included_monthly":"1000.0000"}
Idempotence check: remaining unchanged = PASS

Ledger entries (should be exactly 1): 1
  {"id":"acl-test-prod-1","delta":"1000.0000","reason":"initial_grant",
   "request_id":"initial_grant_sub_test_ph3315_idempotence"}

CLEANUP: all test data removed
Verify cleanup: tenants remaining = 0
```

### Preuve ecomlg-001 non touché

```
ai_actions_wallet:
  tenant_id: ecomlg-001
  remaining: 901.48 (inchangé)
  included_monthly: 1000
  purchased_remaining: 0
  reset_at: 2026-03-01

Wallet endpoint /ai/wallet/status:
  remaining: 901.48
  includedMonthly: 1000
  used7d: 41.96
```

### Preuve ledger ecomlg-001 (dernières entrées — aucune `initial_grant` ajoutée)

```
acl-mlkmpuk7fwtrai  delta=-9.65   reason=ai_generation  2026-02-13
acl-mlhctkwmylo9yu  delta=-5.77   reason=ai_generation  2026-02-11
acl-mlgs5c60ebx8s6  delta=-10.16  reason=ai_generation  2026-02-10
(pas de ligne initial_grant — correct, ecomlg-001 n'a pas de billing_subscription)
```

---

## Résumé des changements déployés

| Fichier | Modification |
|---|---|
| `config/kbactions.ts` | `getPlanIncludedKBActions`: case-insensitive (`.toLowerCase()`) + alias `autopilot`=2000 |
| `services/ai-actions.service.ts` | Nouvelle fonction `grantInitialKBActions()` (idempotente via ledger) |
| `modules/billing/routes.ts` | Appel `grantInitialKBActions` dans `handleSubscriptionChange` |

---

## Comportement attendu

Lors du prochain onboarding (Stripe `customer.subscription.created`/`updated`) :
1. `handleSubscriptionChange` crée/met à jour `billing_subscriptions`
2. Met à jour `tenants.plan`
3. **Nouveau** : appelle `grantInitialKBActions(tenantId, plan, subscriptionId)`
4. Le wallet est crédité avec le montant du plan (PRO=1000, AUTOPILOT=2000)
5. Le ledger contient une entrée `initial_grant` avec `request_id=initial_grant_{subId}`
6. Si le webhook est rejoué → idempotent, pas de double crédit
