# PH28.23B - Settings Company Profile API Integration Report

**Date:** 2026-02-03
**Status:** ✅ COMPLET
**Environments:** DEV + PROD

---

## Objectifs

1. Paramètres → Entreprise lit depuis `GET /tenant-context/profile/:tenantId`
2. Sauvegarde via `PUT /tenant-context/profile/:tenantId`
3. Persistance après refresh
4. Suppression du défaut "Ma Boutique"

---

## Implémentation

### 1. Nouvelle API Route Client

**Fichier créé:** `app/api/tenant-context/profile/[tenantId]/route.ts`

```typescript
// GET - Fetch profile from backend
export async function GET(request, { params }) {
  const { tenantId } = await params;
  const response = await fetch(`${API_URL}/tenant-context/profile/${tenantId}`, {
    headers: { 'X-User-Email': userEmail },
  });
  return NextResponse.json(data);
}

// PUT - Update profile in backend
export async function PUT(request, { params }) {
  const { tenantId } = await params;
  const body = await request.json();
  const response = await fetch(`${API_URL}/tenant-context/profile/${tenantId}`, {
    method: 'PUT',
    body: JSON.stringify(body),
  });
  return NextResponse.json(data);
}
```

### 2. Modifications Settings Page

**Fichier:** `app/settings/page.tsx`

**Changements:**

1. **Default profile vide** (plus de "Ma Boutique"):
```typescript
const defaultProfile: BusinessProfile = {
  companyName: "",
  email: "",
  phone: "",
  returnAddress: ""
};
```

2. **Nouveaux états:**
```typescript
const [profileLoading, setProfileLoading] = useState(true);
const [profileError, setProfileError] = useState<string | null>(null);
```

3. **useEffect pour fetch profile:**
```typescript
useEffect(() => {
  if (!currentTenantId) return;
  
  const fetchProfile = async () => {
    const res = await fetch(`/api/tenant-context/profile/${currentTenantId}`);
    if (res.ok) {
      const data = await res.json();
      setProfile({
        companyName: data.profile.companyName || data.tenant?.name || '',
        email: data.profile.supportEmail || '',
        // ...
      });
    }
  };
  fetchProfile();
}, [currentTenantId]);
```

4. **saveSettings appelle l'API:**
```typescript
const saveSettings = useCallback(async () => {
  // Save to localStorage for other settings
  localStorage.setItem(STORAGE_KEY, JSON.stringify(data));
  
  // PH28.23B: Save profile to API
  if (effectiveTenantId) {
    await fetch(`/api/tenant-context/profile/${effectiveTenantId}`, {
      method: 'PUT',
      body: JSON.stringify({
        companyName: profile.companyName,
        supportEmail: profile.email,
      }),
    });
  }
  setToast("Parametres enregistres");
}, [...]);
```

---

## Tests E2E DEV

### Test 1: Lecture profil (tenant neuf)

| Champ | Avant (PH28.23) | Après (PH28.23B) |
|-------|-----------------|------------------|
| Nom société | "Ma Boutique" ❌ | "OTP Signup Test" ✅ |
| Email | "support@maboutique.com" ❌ | "contact+otpsign1@ecomlg.fr" ✅ |
| Téléphone | "+33 1 23 45 67 89" ❌ | (vide) ✅ |

### Test 2: Modification + Sauvegarde

1. Modifier email: `contact+otpsign1@ecomlg.fr` → `support-test@ecomlg.fr`
2. Cliquer "Enregistrer"
3. Network: `PUT /api/tenant-context/profile/otp-signup-test-ml5oo0qu` ✅

### Test 3: Persistance après refresh

1. Refresh page (F5)
2. Email affiché: `support-test@ecomlg.fr` ✅
3. Network: `GET /api/tenant-context/profile/otp-signup-test-ml5oo0qu` ✅

---

## Network Proof

```
[GET] /api/tenant-context/profile/otp-signup-test-ml5oo0qu  → 200
[PUT] /api/tenant-context/profile/otp-signup-test-ml5oo0qu → 200
```

---

## Déploiement

| Env | Image | Status |
|-----|-------|--------|
| DEV | `ghcr.io/keybuzzio/keybuzz-client:dev-settings-profile-2026-02-03` | ✅ Déployé |
| PROD | `ghcr.io/keybuzzio/keybuzz-client:prod-settings-profile-2026-02-03` | ✅ Déployé |

**Digest:** `sha256:687dd975ed926e74a838f8f7980c79383b1549ccf0376ac10855b679d6040d35`

---

## Résumé

| Objectif | Status |
|----------|--------|
| GET profile depuis API | ✅ |
| PUT profile vers API | ✅ |
| Persistance après refresh | ✅ |
| Plus de "Ma Boutique" par défaut | ✅ |
| Empty state si profil vide | ✅ |
| TenantId obligatoire | ✅ |

---

## Notes Techniques

- Le `currentTenantId` provient de `useTenant()` (TenantProvider)
- Les autres paramètres (horaires, congés, etc.) restent en localStorage
- Seul le profil entreprise (companyName, supportEmail) est synchronisé avec l'API

**PH28.23B terminé avec succès.**
