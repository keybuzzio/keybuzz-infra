# PH151.1-STEP4-PRIORITY-UX-CONSOLIDATION-01

> Date : 16 avril 2026
> Environnement : DEV uniquement
> Baseline : v3.5.73-ph151-step3.2-merge-dev (commit 8fc6835)
> Image : `v3.5.74-ph151-step4-priority-dev`
> Commit : `1765d03`

---

## Objectif

Remplacer le PriorityBadge textuel par un PriorityDot compact dans la liste de conversations, compacter le bloc stats priorite, et nettoyer la redondance visuelle — SANS casser le tri, les badges SAV/escalade, ni l'information critique.

---

## Audit initial


| Element          | Fichier                      | Etat avant                                            |
| ---------------- | ---------------------------- | ----------------------------------------------------- |
| PriorityBadge    | `PriorityBadge.tsx`          | Badge textuel avec icone + label (10px)               |
| PriorityDot      | `PriorityBadge.tsx`          | Existe deja (w-2 h-2 rounded-full, rouge/orange/gris) |
| Usage dans liste | `InboxTripane.tsx:1289`      | IIFE inline, APRES AssignmentBadge et EscalationBadge |
| Stats priorite   | `InboxTripane.tsx:1047-1062` | Bloc rose expansif (2 lignes, fond bg-red-50)         |
| Toggle tri       | `InboxTripane.tsx:1064-1076` | Bloc gris avec toggle "Prioritaires d'abord"          |
| Tri              | `conversationPriority.ts`    | `sortByPriority()` — NON MODIFIE                      |


---

## Modifications appliquees

### 1. Import PriorityDot (remplace PriorityBadge)

```diff
- import { PriorityBadge } from "@/src/features/inbox/components/PriorityBadge";
+ import { PriorityDot } from "@/src/features/inbox/components/PriorityBadge";
```

### 2. Integration dans la liste conversations

**Avant** : PriorityBadge textuel en derniere position (apres AssignmentBadge + EscalationBadge)

**Apres** : PriorityDot en premiere position (avant AssignmentBadge + EscalationBadge)

```diff
+                 {(() => { const p = getConversationPriority(conv, currentUser?.id ?? null); return p.level !== 'low' ? <PriorityDot level={p.level} /> : null; })()}
                  <AssignmentBadge assignedType={conv.assignedType || 'ai'} />
                  <EscalationBadge status={conv.escalationStatus} />
-                 {(() => { const p = getConversationPriority(conv, currentUser?.id ?? null); return p.level !== 'low' ? <PriorityBadge level={p.level} label={p.label} /> : null; })()}
```

Mapping PriorityDot :


| Level  | Couleur                 | Rendu              |
| ------ | ----------------------- | ------------------ |
| high   | rouge (`bg-red-500`)    | point 2x2 rouge    |
| medium | orange (`bg-amber-400`) | point 2x2 orange   |
| low    | -                       | rien (return null) |


### 3. Stats priorite compactees

**Avant** : bloc rose 2 lignes (titre + chiffres)

**Apres** : bloc 1 ligne (icone + titre + chiffres inline)

```diff
- <div className="p-3 bg-red-50 dark:bg-red-900/10 border border-red-200 ...">
-   <div className="flex items-center gap-2 mb-1.5">...</div>
-   <div className="flex items-center gap-3 text-xs">...</div>
- </div>
+ <div className="flex items-center gap-3 px-3 py-2 bg-red-50/60 dark:bg-red-900/10 border border-red-100 ... text-xs">
+   <AlertTriangle .../> <span>Prioritaires</span> <span>N urgentes</span> <span>M moyennes</span>
+ </div>
```

### 4. Nettoyage

- Indentation EscalationBadge alignee (6 espaces en trop corrigees)
- Fichier `conversationPriority.ts` : NON MODIFIE (interdit)
- Fichier `PriorityBadge.tsx` : NON MODIFIE (PriorityDot existait deja)

---

## Fichiers modifies


| Fichier                      | Lignes    | Action               |
| ---------------------------- | --------- | -------------------- |
| `app/inbox/InboxTripane.tsx` | +13 / -17 | Import, badge, stats |


## Fichiers NON modifies (preserves)

- `src/features/inbox/utils/conversationPriority.ts` — tri et logique priorite intacts
- `src/features/inbox/components/PriorityBadge.tsx` — PriorityDot existait deja
- `src/features/inbox/components/EscalationPanel.tsx` — EscalationBadge intact
- `src/features/inbox/components/AssignmentPanel.tsx` — AssignmentBadge intact

---

## Validation


| Test                                | Resultat                   |
| ----------------------------------- | -------------------------- |
| PriorityDot visible (points rouges) | OK                         |
| PriorityDot position (avant badges) | OK                         |
| Conversations sans priorite (low)   | OK — pas de dot            |
| Stats compactes 1 ligne             | OK                         |
| Toggle "Prioritaires d'abord"       | OK — present               |
| Tri fonctionnel                     | OK — sortByPriority intact |
| Badges SAV                          | OK                         |
| Badges Escalade                     | OK                         |
| Badges IA/Assignment                | OK                         |
| Sidebar (Resume du dossier)         | OK                         |
| Console errors                      | 0 nouvelles                |


## Non-regression


| Page                  | Resultat |
| --------------------- | -------- |
| Inbox (conversations) | OK       |
| Inbox (sidebar)       | OK       |
| Dashboard             | OK       |
| /start                | OK       |
| Settings              | OK       |
| Billing               | OK       |
| Channels              | OK       |


---

## Rollback

```bash
kubectl set image deploy/keybuzz-client keybuzz-client=ghcr.io/keybuzzio/keybuzz-client:v3.5.73-ph151-step3.2-merge-dev -n keybuzz-client-dev
```

---

## Verdict : SUCCESS

STOP