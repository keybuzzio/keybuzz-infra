# PH147.RULES-CURSOR-PROCESS-LOCK-01

> Date : 15 avril 2026
> Auteur : Agent Cursor
> Type : documentation / rules uniquement
> Verdict : **CURSOR PROCESS RULES LOCKED**

---

## Rules path

```
c:\DEV\KeyBuzz\V3\.cursor\rules\process-lock.mdc
```

- Format : `.mdc` (Cursor rule standard)
- Frontmatter : `alwaysApply: true` — charge automatiquement a chaque conversation
- Position : 10e rule dans `.cursor/rules/` (9 existantes + 1 ajoutee)

---

## Contenu enregistre

La rule `process-lock.mdc` contient 9 sections :


| #   | Section              | Ce qu'elle impose                                                          |
| --- | -------------------- | -------------------------------------------------------------------------- |
| 1   | Source de verite     | Git unique — interdit copie pod/runtime/dist/SCP                           |
| 2   | Lieu de build        | Un seul endroit — repo/branche/commit/machine documentes                   |
| 3   | Repo clean           | Zero fichier dirty avant build — verification obligatoire                  |
| 4   | Commit/push/PR/merge | Atomique, message explicite, pas de merge large, pas de patch "au passage" |
| 5   | Deploiement          | Un build max par phase, un deploy max, DEV d'abord, rollback prepare       |
| 6   | Rapports             | Image avant/apres, rollback, branche/commit, lieu build, validation reelle |
| 7   | Stop conditions      | Page blanche, repo dirty, divergence Git/runtime, doute source             |
| 8   | Validation humaine   | Aucun GO definitif sans Ludovic, build reussi != validation produit        |
| 9   | Checklist            | 10 points obligatoires par phase                                           |


### Complementarite avec les rules existantes


| Rule existante            | Ce qu'elle couvre                          | Ce que `process-lock` ajoute                                           |
| ------------------------- | ------------------------------------------ | ---------------------------------------------------------------------- |
| `git-source-of-truth.mdc` | Workflow Git, interdiction build direct    | Interdit copie depuis runtime/pod, reverse engineering, SCP source     |
| `deployment-safety.mdc`   | Build/deploy safety, tags, rollback GitOps | Limite 1 build/1 deploy par phase, stop conditions, validation humaine |


Pas de duplication : `process-lock.mdc` reference les deux rules existantes et complete les zones non couvertes.

---

## Fichiers crees


| Fichier                                                    | Type        | Contenu                              |
| ---------------------------------------------------------- | ----------- | ------------------------------------ |
| `.cursor/rules/process-lock.mdc`                           | Cursor rule | Process lock anti-drift (198 lignes) |
| `keybuzz-infra/docs/PH147.RULES-CURSOR-PROCESS-LOCK-01.md` | Rapport     | Ce document                          |


---

## Impact

- **AUCUNE modification produit** (zero fichier dans `app/`, `src/`, `keybuzz-api/`, `keybuzz-client/`)
- **AUCUN build** effectue
- **AUCUN deploy** effectue
- **AUCUN changement infra** (zero manifest K8s, zero configmap, zero secret)
- **AUCUN push PROD**
- **AUCUN push Git** (la rule est locale dans le workspace Cursor)

---

## Verification


| Check                       | Resultat                                                   |
| --------------------------- | ---------------------------------------------------------- |
| Fichier cree                | `.cursor/rules/process-lock.mdc` — 198 lignes              |
| Format correct              | frontmatter YAML + markdown — identique aux 9 autres rules |
| `alwaysApply: true`         | oui — charge a chaque conversation                         |
| Visible dans la liste rules | oui — 10e fichier `.mdc` dans `.cursor/rules/`             |
| Contenu lisible             | francais clair, sections numerotees, tableaux              |
| Aucun code produit modifie  | confirme — seuls des `.mdc` et `.md` crees                 |


---

## Conclusion

**Verdict : CURSOR PROCESS RULES LOCKED**

- Rule `process-lock.mdc` creee et persistee dans `.cursor/rules/`
- `alwaysApply: true` garantit le chargement automatique a chaque session
- Couvre les 8 principes demandes : source de verite, lieu de build, repo clean, commit/push/PR/merge, deploiement, rapports, stop conditions, validation humaine
- Complete (sans dupliquer) les rules existantes `git-source-of-truth.mdc` et `deployment-safety.mdc`
- Aucune modification produit, aucun build, aucun deploy

STOP