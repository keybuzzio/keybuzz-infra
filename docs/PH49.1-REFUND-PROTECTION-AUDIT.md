# PH49.1 — Refund Protection Layer — Audit Prealable

> Date : 9 mars 2026

## Pipeline IA actuel (ordre d'injection dans le prompt)

| # | Couche | Phase | Fichier |
|---|--------|-------|---------|
| 1 | Base prompt | - | `ai-assist-routes.ts` buildSystemPrompt |
| 2 | SAV Policy | PH41 | `sav-policy.ts` |
| 3 | Tenant Policy | PH44 | `tenantPolicyLoader.ts` |
| 4 | Historical Engine | PH43 | `historicalResolutionEngine.ts` |
| 5 | Decision Tree | PH45 | `sav-decision-tree.ts` |
| 6 | Response Strategy | PH46 | `responseStrategyEngine.ts` |
| 7 | **Refund Protection** | **PH49** | **`refundProtectionLayer.ts`** (NOUVEAU) |
| 8 | Order Context | PH44.7 | `ai-assist-routes.ts` buildOrderContextSummary |
| 9 | Supplier Context | PH32 | `ai-assist-routes.ts` |
| 10 | Tenant Rules | PH32 | `ai-assist-routes.ts` |

## Donnees deja dans le pipeline

| Donnee | Disponible | Source |
|--------|-----------|--------|
| scenario SAV | OUI | PH41 `savClassifier.ts` |
| confidence decision | OUI | PH45 `sav-decision-tree.ts` |
| signals (tracking, photos, etc.) | OUI | PH45 `buildSignalsFromContext()` |
| anti-patterns | OUI | PH43 `historicalResolutionEngine.ts` |
| tenant policy (supplier_first, require_photos) | OUI | PH44 `tenantPolicyLoader.ts` |
| customer risk | OUI | PH47 `customerRiskEngine.ts` |
| order value category | OUI | PH48 `productValueAwareness.ts` |
| response strategy | OUI | PH46 `responseStrategyEngine.ts` |

## Point d'injection identifie

**Apres PH46 (Response Strategy) et avant Order Context.**

Raison : le Refund Protection Layer est le dernier filtre decisionnef avant que le prompt final soit envoye au LLM. Il doit avoir acces a toutes les couches precedentes.

## Entrees du Refund Protection Layer

- `scenario` (SavScenario) — de PH41
- `signals` (AvailableSignals) — de PH45 buildSignalsFromContext
- `customerRiskCategory` — de PH47
- `orderValueCategory` — de PH48

## Sortie

```typescript
{
  refundAllowed: boolean,
  refundConfidence: 'high' | 'medium' | 'low',
  protectionReason: string,
  requiredPreconditions: string[],
  recommendedAlternative: string,
  triggeredRules: string[],
  promptBlock: string,
}
```

## Regles de protection identifiees (10 regles)

1. `delivery_not_clarified` — livraison non clarifiee (poids 3)
2. `delivery_window_active` — fenetre de livraison non depassee (poids 4)
3. `missing_photos` — preuves photos manquantes (poids 3)
4. `high_value_order` — commande HIGH/CRITICAL_VALUE (poids 2)
5. `risky_customer` — client RISKY (poids 4)
6. `anti_pattern_refund_first` — anti-pattern remboursement premature (poids 5)
7. `tenant_supplier_first` — policy tenant: fournisseur d'abord (poids 3)
8. `tenant_requires_photos` — policy tenant: photos requises (poids 3)
9. `tenant_diagnostic_required` — policy tenant: diagnostic obligatoire (poids 3)
10. `no_order_context` — commande non identifiee (poids 2)

## Tests prevus

12 scenarios en mode simulation (pas de conversation reelle, pas de marketplace).
