# PH151.3A — Priority Dot Micro-Recovery

> **Date** : 14 avril 2026
> **Environnement** : DEV uniquement
> **Image** : `ghcr.io/keybuzzio/keybuzz-client:v3.5.64-ph151.3a-priority-dot-dev`
> **Rollback** : `v3.5.63-ph151.2-case-summary-clean-dev`

---

## Verdict

**PRIORITY DOT MICRO-RECOVERY SUCCESSFUL**

---

## Préflight

- Image DEV de départ : `v3.5.63-ph151.2-case-summary-clean-dev` (confirmée)
- `git status` avant : `InboxTripane.tsx` contenait 243 lignes de contamination PH151
- Action préflight : `git checkout -- app/inbox/InboxTripane.tsx` pour reset propre
- `git diff --stat` après reset : 0 changements (fichier identique au commit HEAD)
- Fichiers PH151 contaminants confirmés non embarqués :
  - `AICaseSummary.tsx` : `??` (untracked)
  - `ConversationSummaryBar.tsx` : `??` (untracked)
  - `MessageBubble.tsx` : `??` (untracked)
  - `MessageFilterToggle.tsx` : `??` (untracked)
  - `messageClassifier.ts` : `??` (untracked)

---

## Action appliquée

**Fichier modifié** : `app/inbox/InboxTripane.tsx` (1 seul fichier)

**Diff exact** (2 insertions, 2 suppressions) :

```diff
-import { PriorityBadge } from "@/src/features/inbox/components/PriorityBadge";
+import { PriorityBadge, PriorityDot } from "@/src/features/inbox/components/PriorityBadge";

-{(() => { const p = getConversationPriority(conv, currentUser?.id ?? null); return p.level !== 'low' ? <PriorityBadge level={p.level} label={p.label} /> : null; })()}
+{(() => { const p = getConversationPriority(conv, currentUser?.id ?? null); return p.level !== 'low' ? <PriorityDot level={p.level} /> : null; })()}
```

**Note** : Le changement "Assigné à moi" → "À moi" n'a pas été appliqué car la version bastion utilise `<AssignmentBadge>` et non un label inline. Le scope final est de 2+2 au lieu de 3+3, ce qui rend le diff encore plus minimal.

**Vérification anti-contamination sur bastion** :

```
grep -c 'MessageBubble\|AICaseSummary\|MessageFilterToggle\|classifyInboundMessage\|ConversationSummaryBar' InboxTripane.tsx
→ 0
```

---

## Validation


| Test                                | Résultat                                       |
| ----------------------------------- | ---------------------------------------------- |
| Inbox charge                        | OK — 378 conversations                         |
| Page blanche                        | Non                                            |
| PriorityDot visible                 | Oui — points rouges sur conversations urgentes |
| Bloc "Prioritaires" préservé        | Oui — "9 urgentes, 40 moyennes" intact         |
| Conversation simple                 | OK                                             |
| Conversation SAV (SAV complexe)     | OK — messages, commande, suggestions IA        |
| Conversation avec escalade (Retour) | OK — badge Escaladé visible                    |
| Conversation avec commande          | OK — panneau commande + livraison + articles   |
| Filtres (Tous, À traiter, Escaladé) | OK                                             |
| Sidebar gauche                      | OK — stats, filtres statut/canal/type/SAV      |
| Messages dans le détail             | OK — rendu identique à PH151.2                 |


---

## Non-régression


| Page                      | Résultat                         |
| ------------------------- | -------------------------------- |
| Inbox (378 conversations) | OK                               |
| Dashboard                 | OK — KPIs, SLA, canaux, activité |
| Orders (11932)            | OK                               |
| Channels (7 Amazon)       | OK                               |
| Billing                   | OK — Plan Pro, KBActions         |
| Settings                  | OK — 10 onglets                  |


---

## Rollback

- Utilisé : Non (pas nécessaire)
- Commande :

```bash
kubectl set image deployment/keybuzz-client keybuzz-client=ghcr.io/keybuzzio/keybuzz-client:v3.5.63-ph151.2-case-summary-clean-dev -n keybuzz-client-dev
```

---

## Conclusion

- **DEV stable** — `v3.5.64-ph151.3a-priority-dot-dev` déployé et validé
- **Aucune autre correction effectuée**
- **Diff chirurgical** : 2 lignes changées, 0 fichier ajouté, 0 contamination PH151
- **GitOps** : `deployment.yaml` mis à jour
- Le label "À moi" (raccourci) n'a pas été appliqué car la structure bastion diffère — à traiter dans une phase ultérieure si souhaité

