apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: keybuzz-ai
spec:
  selector:
    app: litellm
  ports:
    - name: http
      port: 80
      targetPort: 4000
  type: ClusterIP

