apiVersion: apps/v1
kind: Deployment
metadata:
  name: keybuzz-backend
  namespace: keybuzz-backend-dev
  labels:
    app: keybuzz-backend
    app.kubernetes.io/instance: keybuzz-backend-dev
    app.kubernetes.io/name: keybuzz-backend
    app.kubernetes.io/part-of: keybuzz
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keybuzz-backend
      app.kubernetes.io/instance: keybuzz-backend-dev
      app.kubernetes.io/name: keybuzz-backend
      app.kubernetes.io/part-of: keybuzz
      environment: dev
  template:
    metadata:
      labels:
        app: keybuzz-backend
        app.kubernetes.io/instance: keybuzz-backend-dev
        app.kubernetes.io/name: keybuzz-backend
        app.kubernetes.io/part-of: keybuzz
        environment: dev
    spec:
      containers:
        - name: keybuzz-backend
          image: ghcr.io/keybuzzio/keybuzz-backend:v1.0.40-ph263-safe-dev
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 4000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "4000"
            - name: JWT_SECRET
              value: keybuzz-dev-jwt-secret-2026
            - name: KEYBUZZ_SUPERADMIN_EMAIL
              value: admin@keybuzz.io
            - name: VAULT_ADDR
              value: "http://vault.default.svc.cluster.local:8200"
            - name: ADMIN_CALLBACK_URL
              value: https://admin-dev.keybuzz.io/inbound-email/amazon/callback
            - name: KEYBUZZ_DEV_MODE
              value: "true"
            - name: DEV_AUTH
              value: "true"
            - name: KEYBUZZ_INTERNAL_PROXY_TOKEN
              valueFrom:
                secretKeyRef:
                  name: keybuzz-internal-proxy
                  key: token
          envFrom:
            - secretRef:
                name: keybuzz-backend-db
            - secretRef:
                name: vault-token
                optional: true
            - secretRef:
                name: amazon-spapi-creds
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: ghcr-cred