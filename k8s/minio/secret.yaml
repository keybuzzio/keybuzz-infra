# SECURITY: MinIO root credentials stored in Vault at keybuzz/minio_root
# Do NOT commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: minio
type: Opaque
stringData:
  MINIO_ROOT_USER: "<FROM_VAULT: keybuzz/minio_root>"
  MINIO_ROOT_PASSWORD: "<FROM_VAULT: keybuzz/minio_root>"
