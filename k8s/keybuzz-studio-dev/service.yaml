apiVersion: v1
kind: Service
metadata:
  name: keybuzz-studio
  namespace: keybuzz-studio-dev
  labels:
    app: keybuzz-studio
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: keybuzz-studio
